/**
 * Provenance Marks（STEP 6）：Tiptap 自訂 marks。
 * aiOrigin{copyEventId,messageId,srcStart,srcEnd}：貼上內容 sha1 命中 Chat 複製記錄。
 * externalOrigin：貼上但未命中（來自系統外部）。手打內容不掛 mark。
 */

// 骨架檔：實作於上述 STEP。
export {};
