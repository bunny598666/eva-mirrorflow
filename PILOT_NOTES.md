# PILOT_NOTES

壓測與試辦的紀錄。BUILD_PLAN §6 STEP 13 要求「修正問題記錄於 PILOT_NOTES.md」。

---

## 2026-08-11｜45 併發壓測（本機）

### 環境

| 項目 | 值 |
|---|---|
| 應用程式 | `next start`（**正式建置**，非 dev server），本機 |
| 資料庫 | Supabase 免費方案，`aws-0-ap-southeast-1`（新加坡） |
| AI | `AI_PROVIDER=mock ALLOW_MOCK_AI=1` |
| 情境 | 45 人 × 3 分鐘，進場時間差 30 秒，每 4 秒一批事件 |

```bash
npm run loadtest -- --students 45 --minutes 3
```

### 結果（三項驗收全過）

| 驗收條件 | 標準 | 實測 |
|---|---|---|
| 事件遺漏率 | 0% | **0.000%**（送出 1997，落庫 1997） |
| 反思送出成功率 | 100% | **100%**（45/45） |
| `/api/events` p95 | < 800ms | **188ms**（p50 131 / p99 316，n=1907） |

HTTP 失敗 **0 / 2334**。交件成功 45/45。

各端點延遲：

| 端點 | n | p50 | p95 | p99 |
|---|---|---|---|---|
| `/api/events` | 1907 | 131ms | 188ms | 316ms |
| `/api/sessions` | 45 | 267ms | 369ms | 433ms |
| `/api/snapshots` | 45 | 426ms | 640ms | 785ms |
| `/api/auth/login` | 45 | 494ms | 557ms | 710ms |
| `/api/reflections` | 45 | 572ms | 939ms | 993ms |
| `/api/chat` | 202 | 649ms | 708ms | 781ms |
| `/api/submit` | 45 | 1609ms | 1979ms | 2099ms |

---

## 壓測過程中發現並修正的問題

### 1. `/api/submit` p50 15.7 秒 —— 象限座標逐列寫入（已修）

**現象**：第一輪壓測 `/api/submit` 的 p50 是 15771ms、p95 是 18470ms。學生會卡在
「交出去」按鈕上將近 20 秒，而下課鐘響前全班會同時按那顆按鈕。

**原因**：象限座標的 z 分數以該期全班為基準，所以任何人交件都要整期重算
（這是正確的，見 `lib/metrics/service.ts` 的說明）。但寫回資料庫時是逐列
「先查有沒有、再決定 insert 或 update」＝每人 2 次往返。45 人同時交件：

```
45 次交件 × 45 位學生 × 2 次查詢 ≈ 4000 次往返新加坡
```

**修正**：`supabase/migrations/011_analyses_unique.sql` 對 `analyses` 加上
`(session_id, kind)` 唯一鍵，改成整期一次 `upsert`。`saveDna` 同樣少掉一次查詢。

**效果**：p50 15771ms → **583ms**（同一輪測試條件下，27 倍）。

### 2. `/api/events` 每次都查一次場次歸屬（已修）

**現象**：`/api/events` 是全系統呼叫最頻繁的端點——45 人同堂、每 4 秒一批，
一節課約兩萬次。每次都做一次「這個場次是不是你的」查詢。

**修正**：`lib/api/session-owner.ts` 加了記憶體快取。場次的擁有者寫入後永遠
不會變（沒有任何程式路徑會改 `sessions.participant_id`，003 的
`guard_session_update` trigger 在資料庫層再擋一次，uuid 也不會被回收），
所以這份快取不會過期。

**效果**：p50 330ms → **168ms**。

### 3. 壓測腳本沿用固定代號，第二輪全部撞 409（測試工具問題，已修）

一位學生一份作業只有一個場次。第二輪跑起來時撈到上一輪**已交件**的場次，
於是 135 次對話全部回 409「這次已經交出去了」。改成每輪用一組新代號
（`LT{runId}-01..45`）。

### 4. 進場時間差：同步尖峰不是真實情境（測試設計調整）

原本 45 個虛擬學生在同一毫秒登入並保持完全同步的 4 秒節奏。真實課堂是陸續
開機、陸續登入、各自的打字節奏。同步尖峰會製造現實不存在的排隊，把 p95 灌到失真。

預設加入 30 秒進場時間差（`--ramp 30`）。要看同步尖峰的最壞情況下 `--ramp 0`。

### 5. 匯出只寫了 1000 列事件 —— PostgREST 的預設上限（已修）★

**這是整個開發過程中最嚴重的一個缺陷。**

壓測讓資料庫的事件數從 56 筆長到 6734 筆，然後 `npm run verify:step12` 紅了：

```
✗ events.csv 列數 === DB events 筆數
    實際 1000
    預期 6734
```

**原因**：PostgREST（Supabase 的 API 層）**預設每次最多回 1000 列**，而且
不報錯——它就是安靜地少給你。

**為什麼差點沒被發現**：manifest 記的筆數與檔案內容是一致的（兩邊都是 1000），
所以「manifest 與檔案相符」那組檢查全部通過。是「檔案列數 === 資料庫 count」
這一條抓到的——當初寫那條檢查時只覺得是多此一舉。

**影響範圍不只匯出**。同一個上限套用在每一個沒有分頁的查詢上：

| 位置 | 後果 |
|---|---|
| `lib/export/bundle.ts` | 匯出的研究資料少 85% |
| `lib/replay/queries.ts` | 單一場次超過 1000 筆事件後，回放靜靜地停住（STEP 7 的驗收目標就是 5000 筆） |
| `lib/metrics/service.ts` | 整期全班的提問訊息被截斷 → 象限座標算錯 |
| `lib/coding/queries.ts` | 編碼者看到的對話不完整 |

**修正**：`lib/supabase/paged.ts` 的 `fetchAllRows()`，以 `.range()` 逐頁取到底。
上述四處全部改用它。每個分頁查詢都必須帶 `.order()`——沒有排序的分頁在
PostgreSQL 裡沒有穩定順序，換頁時會重複或漏掉。

**教訓**：「A 與 B 一致」型的檢查，兩邊都要是獨立來源。拿自己算的數字跟
自己寫的檔案比，永遠會過。

### 6. dev server 的數字不能拿來當驗收（測量方法）

同一組程式碼在 `next dev` 下 `/api/events` p95 是 940ms，在 `next start`
（正式建置）下是 188ms。**壓測必須跑正式建置**，dev server 有 HMR、source map、
未最佳化的模組載入，量到的是開發工具的成本，不是系統的成本。

### 7. mock provider 的正式環境鎖有效（非問題，記錄備查）

第一次用 `next start` 跑壓測時，203 次對話全部回 503。原因是 `lib/ai/mock.ts`
在 `NODE_ENV=production` 下會拒絕載入（除非 `ALLOW_MOCK_AI=1`）——那道鎖是
刻意設計的，防止假 AI 被誤用到真實課堂。壓測時要明確加上該環境變數。

---

## 尚待完成（正式課堂前必須處理）

### 環境與資料

- [ ] **換一個乾淨的 Supabase 專案。** 目前這個開發專案裡有大量測試殘留
      （`S4-*`、`S9-*`、`LT*-*` 帳號與它們的場次），而 events / chat_messages /
      reflections / snapshots / export_audit 全都是 append-only，**永遠刪不掉**。
      新專案跑 `npm run db:setup` 一次到位，再用 `npm run db:setup -- --check`
      確認 13 張表、7 個 trigger、RLS 全部就位。
- [ ] Vercel 環境變數補上 `REFLECTION_PROMPT_VERSION=v1`。
- [ ] `AUTH_JWT_SECRET` 用 `npm run gen:secret` 產生一組**與本機不同**的。
- [ ] 測試帳號（S-01/02/03=111111、T-01=222222、R-01=333333）全部作廢，
      用 `npm run create:participant` 重建，PIN 只印一次。
- [ ] 開發期間在 Vercel 開啟 Deployment Protection。

### 研究材料（四份，需指導教授確認後凍結）

以教師或研究者身分登入 `/admin/materials` 可以一次看完並列印。

- [ ] AI 助教的 system prompt `v1`
- [ ] 反思題目 `v1`（三題，各最少 30 字）
- [ ] 高階提問判定規則 `q-v1`
- [ ] 人工編碼架構 `scheme-v1`

一旦開始收第一期資料就不得變動（CLAUDE.md 鐵則三）。

### 待人工驗收的項目

- [ ] **STEP 3**：Lighthouse 無障礙分數（目標 ≥90）、真實 iPad 實測
- [ ] **STEP 4**：真實 Anthropic API 路徑（目前只驗過 mock）
- [ ] **STEP 8**：非資訊背景成人 10 秒內能說出三色意義（至少 3 人）
- [ ] **STEP 9**：看鏡子 gate 與反思斷網暫存（自動化環境的分頁不合成畫面，驗不了）
- [ ] **STEP 10**：匯出的 SVG 用 Illustrator／Inkscape 開一次

程序都寫在 README 對應章節。

### 壓測本身

- [ ] 對**部署後的 Vercel 網址**再跑一次（`--base https://…`）。本機測的是
      「應用程式邏輯 + 跨區資料庫」，量不到 Vercel 的冷啟動與函式併發上限。
- [ ] BUILD_PLAN 指定的完整時長：`npm run loadtest -- --minutes 30`。
      本次跑 3 分鐘，足以看出上述兩個效能問題，但看不出長時間的記憶體累積或
      連線池耗竭。

---

## 真實課堂試辦

**尚未進行。** 驗收條件是「真實課堂試辦一次，全班完成完整迴圈」——
這一項只有你能做，程式這邊已經沒有阻擋。

試辦時建議記錄：

- 學生看到 DNA 條碼的第一反應（放聲思考法，對應 §7 風險表的「學生看懂鏡子」）
- 反思題目是不是問得出東西，還是收到場面話
- 45 人同時登入的實際體感（壓測量的是伺服器，量不到教室的 Wi-Fi）
- 一節課的時間夠不夠走完「寫作 → 交件 → 看鏡子 → 反思」
